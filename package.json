{
  "name": "3d-world",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack project (React + Tailwind CSS frontend, Node.js + Express backend)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build:client": "npm run build --prefix client"
  }
}
